# Architecture Overview

Matcha uses NestJS modules as bounded contexts. The documentation set should explain:

- request entrypoints
- module ownership
- persistence/projection boundaries
- discovery and goals flows
- media flow
- auth and session lifecycle

Use ADRs for decisions that are not obvious from code.

